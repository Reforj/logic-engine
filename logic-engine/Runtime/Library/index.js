import MathScope from './MathScope'
import LogicScope from './LogicScope'
import ArrayScope from './ArrayScope'
import DebugScope from './DebugScope'

export default {
  Math: MathScope,
  Logic: LogicScope,
  Array: ArrayScope,
  Debug: DebugScope,
}
